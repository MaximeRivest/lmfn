name: release

# Publish lmfn to PyPI when a GitHub Release is published.
# PyPI Trusted Publishing (OIDC): no stored tokens. The publish action
# uploads PEP 740 attestations. Actions pinned to commit SHAs (same pins
# as lm15-python's release workflow).

on:
  release:
    types: [published]

permissions: {}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Check tag matches the package version
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          VERSION=$(python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])")
          if [ "$TAG" != "$VERSION" ]; then
            echo "::error::tag $GITHUB_REF_NAME does not match version $VERSION"; exit 1
          fi
      - name: Build sdist and wheel
        run: |
          python -m pip install --quiet build==1.3.0 twine==6.2.0
          python -m build --outdir dist
          python -m twine check --strict dist/*
      - name: Smoke-test the wheel
        run: |
          python -m pip install "$(ls dist/*.whl)[verifiers]"
          cd /tmp && python -I -c "import lmfn, lmfn_verifiers, importlib.metadata as m, os; assert lmfn.__version__ == m.version('lmfn') == os.environ['GITHUB_REF_NAME'].removeprefix('v')"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: dist
          path: dist/
          if-no-files-found: error

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/lmfn/
    permissions:
      id-token: write # PyPI Trusted Publishing (OIDC)
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@ed0c53931b1dc9bd32cbe73a98c7f6766f8a527e # v1.13.0
